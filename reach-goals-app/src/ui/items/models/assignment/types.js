/**
 * @typedef {import('../../elements/card/types.js').CardProps} CardProps
 */

/**
 * @typedef {import('../../elements/card-mini/types.js').CardMiniProps} CardMiniProps
 */

/**
 * @typedef {Object} CardDraggableProps
 * @property {string | number} itemID
 * @property {number} index
 */

/**
 * @typedef {CardProps | CardMiniProps | CardDraggableProps} CardRenderProps
 */

/**
 * @typedef {Object} AssignmentProps
 * @property {string} [status]
 * @property {import('../../../../utils/types.js').DisplayProps} display
 * @property {Array} source
 * @property {boolean} [selectableModel]
 * @property {boolean} [detailsModel]
 * @property {boolean} [draggable]
 * @property {boolean} [checkboxModel]
 * @property {boolean} [showTags]
 */

export {}